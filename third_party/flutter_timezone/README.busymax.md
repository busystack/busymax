# BusyMax flutter_timezone packaging

This directory contains the Dart API and Windows implementation from upstream
`flutter_timezone` 5.1.0 under its original license. Other platform plugin
registrations are intentionally omitted because BusyMax Linux retains its
existing `/etc/timezone`, `/etc/localtime`, and GWeather behavior.
